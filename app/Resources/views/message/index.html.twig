{% extends 'base.html.twig' %}

{% block body %}
    <h1>Message list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Projectid</th>
                <th>Parentmessageid</th>
                <th>Content</th>
                <th>Parentfileid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td><a href="{{ path('message_show', { 'id': message.id }) }}">{{ message.id }}</a></td>
                <td>{{ message.projectId }}</td>
                <td>{{ message.parentMessageId }}</td>
                <td>{{ message.content }}</td>
                <td>{{ message.parentFileId }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('message_show', { 'id': message.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('message_edit', { 'id': message.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('message_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
